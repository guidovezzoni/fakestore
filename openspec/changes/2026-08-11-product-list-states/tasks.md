## 1. MVI Contract Restructuring (Prerequisites)

- [ ] 1.1 Restructure `app/src/main/java/.../ui/state/ProductListUiState.kt` from a flat data class into a `sealed interface ProductListUiState` with three variants: `data object Loading`, `data class Content(val products: List<ProductListItem>)`, `data object Error`
- [ ] 1.2 Add `data object RetryClicked : ProductListUiIntent` to `app/src/main/java/.../ui/intent/ProductListUiIntent.kt`, alongside the existing `LoadProducts`
- [ ] 1.3 Update `PreviewProductListScreen` in `ProductListScreen.kt` to construct `ProductListUiState.Content(products = ...)` instead of the old flat `ProductListUiState(products = ...)` constructor

## 2. ProductListViewModel — Loading, Content, Error, Retry (BDD)

- [ ] 2.1 Write test: GIVEN a newly constructed `ProductListViewModel` WHEN no intent has been dispatched THEN `uiState.value` is `ProductListUiState.Loading` (replaces the existing "products equals emptyList" initial-state test), in `ProductListViewModelTest`
- [ ] 2.2 Implement: change `_uiState`'s initial value from `ProductListUiState()` to `ProductListUiState.Loading` in `ProductListViewModel`
- [ ] 2.3 Write test: GIVEN a mocked `GetProductsUseCase` returning `Result.success` with a list of products WHEN `LoadProducts` is dispatched THEN `uiState.value` is `ProductListUiState.Content` with one mapped `ProductListItem` per product, in order (updates the existing mapped-products test to the sealed type), in `ProductListViewModelTest`
- [ ] 2.4 Write test: GIVEN a mocked `GetProductsUseCase` returning `Result.success` with an empty list WHEN `LoadProducts` is dispatched THEN `uiState.value` equals `ProductListUiState.Content(products = emptyList())`, in `ProductListViewModelTest`
- [ ] 2.5 Write test: GIVEN a mocked `GetProductsUseCase` returning `Result.failure` with a technical exception (e.g. an `IllegalStateException` whose message contains exception-class-like detail) WHEN `LoadProducts` is dispatched THEN `uiState.value` is `ProductListUiState.Error`, a data object with no fields, so no exception detail can be represented in the UI state, in `ProductListViewModelTest`
- [ ] 2.6 Implement: in `ProductListViewModel.loadProducts()`, set `_uiState.value = ProductListUiState.Loading` before invoking `getProductsUseCase()`, and add `result.onFailure { _uiState.value = ProductListUiState.Error }` alongside the existing `onSuccess` mapping to `ProductListUiState.Content`
- [ ] 2.7 Write test: GIVEN a mocked `GetProductsUseCase` returning a fixed successful product list WHEN `LoadProducts` is dispatched twice THEN `uiState.value` is still `ProductListUiState.Content` with the mapped list and no duplicated entries (updates the existing dedup test to the sealed type), in `ProductListViewModelTest`
- [ ] 2.8 Write test: GIVEN a mocked `GetProductsUseCase` returning products with known price and rating values WHEN `LoadProducts` is dispatched THEN the resulting `Content` item's `formattedPrice` and `formattedRatingScore` equal `ProductListFormatter`'s output for the same inputs (updates the existing formatting test to the sealed type), in `ProductListViewModelTest`
- [ ] 2.9 Write test: GIVEN a mocked `GetProductsUseCase` that returns `Result.failure` on its first invocation and `Result.success` with a product list on its second invocation WHEN `LoadProducts` is dispatched followed by `RetryClicked` THEN `uiState.value` transitions from `ProductListUiState.Error` to `ProductListUiState.Content` with the mapped products from the second invocation, in `ProductListViewModelTest`
- [ ] 2.10 Write test: GIVEN a mocked `GetProductsUseCase` whose `invoke()` continues to return `Result.failure` WHEN `RetryClicked` is dispatched from an `Error` state THEN `uiState.value` remains `ProductListUiState.Error`, in `ProductListViewModelTest`
- [ ] 2.11 Implement: route `ProductListUiIntent.RetryClicked` to the same `loadProducts()` function in `ProductListViewModel.onIntent()`, alongside the existing `LoadProducts` branch

## 3. ProductListScreen — State Rendering (BDD)

- [ ] 3.1 Add new test tag constants to `ProductListScreen.kt`: `PRODUCT_LIST_LOADING_INDICATOR_TEST_TAG`, `PRODUCT_LIST_ERROR_CONTAINER_TEST_TAG`, `PRODUCT_LIST_RETRY_BUTTON_TEST_TAG`, `PRODUCT_LIST_EMPTY_MESSAGE_TEST_TAG` (prerequisite for the tests below, following the existing `PRODUCT_LIST_ITEM_CARD_TEST_TAG_PREFIX` pattern)
- [ ] 3.2 Write test: GIVEN `uiState` is `ProductListUiState.Loading` WHEN the stateless `ProductListScreen` is composed THEN the node tagged `PRODUCT_LIST_LOADING_INDICATOR_TEST_TAG` is displayed and no `product_list_item_card_*` tagged nodes exist (updates the existing empty-list test to the sealed type), in `ProductListScreenTest`
- [ ] 3.3 Write test: GIVEN `uiState` is `ProductListUiState.Content` with a non-empty products list WHEN the stateless `ProductListScreen` is composed THEN one `ProductListItemCard` is displayed per item (updates the existing "one card per product" test to the sealed type), in `ProductListScreenTest`
- [ ] 3.4 Write test: GIVEN `uiState` is `ProductListUiState.Content(products = emptyList())` WHEN the stateless `ProductListScreen` is composed THEN the node tagged `PRODUCT_LIST_EMPTY_MESSAGE_TEST_TAG` is displayed and no `product_list_item_card_*` tagged nodes exist, in `ProductListScreenTest`
- [ ] 3.5 Write test: GIVEN `uiState` is `ProductListUiState.Error` WHEN the stateless `ProductListScreen` is composed THEN the error message text and the node tagged `PRODUCT_LIST_RETRY_BUTTON_TEST_TAG` are both displayed, in `ProductListScreenTest`
- [ ] 3.6 Write test: GIVEN `uiState` is `ProductListUiState.Error` WHEN the retry button is tapped THEN the captured intents list contains `ProductListUiIntent.RetryClicked`, in `ProductListScreenTest`
- [ ] 3.7 Write test: GIVEN `uiState` is, in turn, `Loading`, `Content` (populated), `Content` (empty), and `Error` WHEN the stateless `ProductListScreen` is composed for each THEN the top app bar title text is displayed in every case, in `ProductListScreenTest`
- [ ] 3.8 Implement: rewrite `ProductListScreen.kt`'s `Scaffold` content lambda as `when (uiState) { ProductListUiState.Loading -> ...; is ProductListUiState.Content -> ...; ProductListUiState.Error -> ... }` — a centred `CircularProgressIndicator` (tagged `PRODUCT_LIST_LOADING_INDICATOR_TEST_TAG`) for `Loading`; for `Content`, the existing `LazyColumn` of `ProductListItemCard`s when `products` is non-empty, otherwise an empty-state `Text` (`stringResource(R.string.product_list_empty_message)`, tagged `PRODUCT_LIST_EMPTY_MESSAGE_TEST_TAG`); for `Error`, a container (tagged `PRODUCT_LIST_ERROR_CONTAINER_TEST_TAG`) with the error message `Text` (`stringResource(R.string.product_list_error_message)`) and a `Button` (tagged `PRODUCT_LIST_RETRY_BUTTON_TEST_TAG`, label `stringResource(R.string.product_list_retry_button)`) that dispatches `onIntent(ProductListUiIntent.RetryClicked)` on click; keep `TopAppBar` unchanged outside the `when`
- [ ] 3.9 Update the existing "LoadProducts intent fires on first composition" test's `uiState` construction from `ProductListUiState()` to `ProductListUiState.Loading`, in `ProductListScreenTest`
- [ ] 3.10 Add `@Preview` composables `PreviewProductListScreenLoading` and `PreviewProductListScreenError`, each `private`, wrapped in `FakeStoreTheme`, alongside the existing `PreviewProductListScreen` (now covering the `Content` variant with products) and a new `PreviewProductListScreenEmpty` covering `Content(products = emptyList())`, so all `ProductListUiState` variants have preview coverage

## 4. Strings and Localisation (Integration)

- [ ] 4.1 Add English string resources to `app/src/main/res/values/strings.xml`: `product_list_error_message` ("Something went wrong. Please try again."), `product_list_retry_button` ("Retry"), `product_list_empty_message` ("No products available")
- [ ] 4.2 Add the corresponding Spanish translations to `app/src/main/res/values-es/strings.xml`: `product_list_error_message` ("Algo ha salido mal. Por favor, inténtalo de nuevo."), `product_list_retry_button` ("Reintentar"), `product_list_empty_message` ("No hay productos disponibles")

## 5. Final Verification

- [ ] 5.1 Run `./gradlew detektDebug` and resolve any violations
- [ ] 5.2 Run `./gradlew test` and confirm every `ProductListViewModelTest` case passes
- [ ] 5.3 Run `./gradlew connectedDebugAndroidTest` and confirm every `ProductListScreenTest` case passes on a connected device or emulator
- [ ] 5.4 Run `./gradlew koverHtmlReportDebug` (or `koverVerify`) and confirm ≥95% coverage on `ProductListViewModel`
- [ ] 5.5 Install and launch the app on a connected device/emulator; verify via `uiautomator dump` and screenshots that the loading indicator appears on launch, the top app bar stays visible across states, and — by forcing a failure (e.g. disabling network connectivity) — the error state with retry button renders and tapping Retry returns to loading then to content
- [ ] 5.6 Cross-check the implementation against every Acceptance Criterion and Definition of Done item in `docs/userstories/1.2.2-Product-List-States-WIP.md`, explicitly noting the AC-8 deviation documented in `design.md` Decision 1
