## 1. Build & Navigation Dependencies (Prerequisites)

- [ ] 1.1 Add `navigationCompose = "2.9.8"` to `[versions]` and `androidx-navigation-compose = { group = "androidx.navigation", name = "navigation-compose", version.ref = "navigationCompose" }` to `[libraries]` in `gradle/libs.versions.toml`
- [ ] 1.2 In `app/build.gradle.kts`: add `alias(libs.plugins.kotlin.serialization)` to the `plugins {}` block, and add `implementation(libs.androidx.navigation.compose)` + `implementation(libs.kotlinx.serialization.json)` to `dependencies {}`
- [ ] 1.3 Create `app/src/main/java/.../ui/navigation/AppDestination.kt` — a `sealed interface AppDestination` with three `@Serializable data object` members: `Products`, `Favourites`, `Profile`

## 2. MainViewModel MVI Contract Types (Prerequisites)

- [ ] 2.1 Create `app/src/main/java/.../ui/state/MainUiState.kt` — `data class MainUiState(val selectedDestination: AppDestination = AppDestination.Products)`
- [ ] 2.2 Create `app/src/main/java/.../ui/intent/MainUiIntent.kt` — `sealed interface MainUiIntent` with one entry: `data class TabTapped(val destination: AppDestination) : MainUiIntent`
- [ ] 2.3 Create `app/src/main/java/.../ui/effect/MainUiEffect.kt` — `sealed interface MainUiEffect` with one entry: `data class NavigateToTab(val destination: AppDestination) : MainUiEffect`

## 3. MainViewModel — Tab Tap Handling (BDD)

- [ ] 3.1 Write test: GIVEN a newly constructed `MainViewModel` WHEN no intent has been dispatched THEN `uiState.value.selectedDestination` equals `AppDestination.Products`, in `MainViewModelTest`
- [ ] 3.2 Implement: create `MainViewModel` (`@HiltViewModel`, `@Inject constructor(private val analyticsClient: AnalyticsClient)`) with `_uiState = MutableStateFlow(MainUiState())`, exposed `uiState`/`uiEffect`, and an `onIntent(intent: MainUiIntent)` dispatcher routing `TabTapped` to a private `onTabTapped(destination)` stub
- [ ] 3.3 Write test: GIVEN a `MainViewModel` whose `uiState.selectedDestination` is `AppDestination.Products` WHEN `MainUiIntent.TabTapped(AppDestination.Favourites)` is dispatched via `onIntent()` THEN `uiState.value.selectedDestination` equals `AppDestination.Favourites`, in `MainViewModelTest`
- [ ] 3.4 Write test: GIVEN a `MainViewModel` whose `uiState.selectedDestination` is `AppDestination.Products` WHEN `MainUiIntent.TabTapped(AppDestination.Products)` is dispatched via `onIntent()` THEN `uiState.value.selectedDestination` remains `AppDestination.Products`, in `MainViewModelTest`
- [ ] 3.5 Implement: in `onTabTapped(destination)`, set `_uiState.value = _uiState.value.copy(selectedDestination = destination)` unconditionally
- [ ] 3.6 Write test: GIVEN a `MainViewModel` with a collector already subscribed to `uiEffect` WHEN `MainUiIntent.TabTapped(AppDestination.Profile)` is dispatched via `onIntent()` THEN the collected effects contain exactly `MainUiEffect.NavigateToTab(AppDestination.Profile)`, in `MainViewModelTest` (collect `uiEffect` into a `mutableListOf` from a `launch`ed coroutine started before dispatching the intent, per the `UnconfinedTestDispatcher` pattern already used in `ProductListViewModelTest`)
- [ ] 3.7 Implement: in `onTabTapped(destination)`, emit `MainUiEffect.NavigateToTab(destination)` via `viewModelScope.launch { _uiEffect.emit(MainUiEffect.NavigateToTab(destination)) }`
- [ ] 3.8 Write test: GIVEN a `MainViewModel` constructed with a mocked `AnalyticsClient` WHEN `MainUiIntent.TabTapped(AppDestination.Products)` is dispatched via `onIntent()` THEN the mocked `AnalyticsClient.logEvent()` is invoked exactly once with `name = "tab_products_tapped"` and empty `params`, in `MainViewModelTest`
- [ ] 3.9 Write test: GIVEN a `MainViewModel` constructed with a mocked `AnalyticsClient` WHEN `MainUiIntent.TabTapped(AppDestination.Favourites)` is dispatched via `onIntent()` THEN the mocked `AnalyticsClient.logEvent()` is invoked exactly once with `name = "tab_favourites_tapped"` and empty `params`, in `MainViewModelTest`
- [ ] 3.10 Write test: GIVEN a `MainViewModel` constructed with a mocked `AnalyticsClient` WHEN `MainUiIntent.TabTapped(AppDestination.Profile)` is dispatched via `onIntent()` THEN the mocked `AnalyticsClient.logEvent()` is invoked exactly once with `name = "tab_profile_tapped"` and empty `params`, in `MainViewModelTest`
- [ ] 3.11 Write test: GIVEN a `MainViewModel` constructed with a mocked `AnalyticsClient`, whose `uiState.selectedDestination` is already `AppDestination.Products` WHEN `MainUiIntent.TabTapped(AppDestination.Products)` is dispatched a second time via `onIntent()` THEN the mocked `AnalyticsClient.logEvent()` is invoked with `name = "tab_products_tapped"` on each of the two dispatches, in `MainViewModelTest`
- [ ] 3.12 Implement: add a private `eventNameFor(destination: AppDestination): String` mapping function and `EVENT_TAB_PRODUCTS_TAPPED`/`EVENT_TAB_FAVOURITES_TAPPED`/`EVENT_TAB_PROFILE_TAPPED` companion constants; call `analyticsClient.logEvent(eventNameFor(destination))` unconditionally at the start of `onTabTapped(destination)`

## 4. BottomNavigationBar — Rendering & Tap Dispatch (BDD)

- [ ] 4.1 Add test tag constants to a new `app/src/main/java/.../ui/screens/BottomNavigationBar.kt`: `BOTTOM_NAVIGATION_PRODUCTS_TAB_TEST_TAG`, `BOTTOM_NAVIGATION_FAVOURITES_TAB_TEST_TAG`, `BOTTOM_NAVIGATION_PROFILE_TAB_TEST_TAG` (prerequisite for the tests below, following the existing `PRODUCT_LIST_*_TEST_TAG` pattern)
- [ ] 4.2 Write test: GIVEN `selectedDestination = AppDestination.Products` WHEN `BottomNavigationBar(selectedDestination, onTabTapped = {})` is composed THEN three tab nodes tagged `BOTTOM_NAVIGATION_PRODUCTS_TAB_TEST_TAG`, `BOTTOM_NAVIGATION_FAVOURITES_TAB_TEST_TAG`, and `BOTTOM_NAVIGATION_PROFILE_TAB_TEST_TAG` are displayed, each showing its localised label text, in `BottomNavigationBarTest`
- [ ] 4.3 Write test: GIVEN `selectedDestination = AppDestination.Favourites` WHEN `BottomNavigationBar` is composed THEN the node tagged `BOTTOM_NAVIGATION_FAVOURITES_TAB_TEST_TAG` reports a selected semantics state and the Products/Profile tab nodes do not, in `BottomNavigationBarTest`
- [ ] 4.4 Write test: GIVEN `selectedDestination = AppDestination.Products` WHEN the user taps the node tagged `BOTTOM_NAVIGATION_FAVOURITES_TAB_TEST_TAG` THEN the captured `onTabTapped` calls list contains `AppDestination.Favourites`, in `BottomNavigationBarTest`
- [ ] 4.5 Write test: GIVEN `selectedDestination = AppDestination.Products` WHEN the user taps the node tagged `BOTTOM_NAVIGATION_PRODUCTS_TAB_TEST_TAG` (the already-selected tab) THEN the captured `onTabTapped` calls list still contains `AppDestination.Products`, in `BottomNavigationBarTest`
- [ ] 4.6 Implement: build the stateless `BottomNavigationBar(selectedDestination: AppDestination, onTabTapped: (AppDestination) -> Unit, modifier: Modifier = Modifier)` composable — a Material 3 `NavigationBar` with three `NavigationBarItem`s (`Icons.Filled.ShoppingCart`/`Icons.Filled.Favorite`/`Icons.Filled.Person`, `stringResource`-sourced labels, the test tag modifiers from 4.1, `selected` computed via equality against `selectedDestination`, `onClick` invoking `onTabTapped` with that item's `AppDestination`); add a `@Preview` `PreviewBottomNavigationBar` wrapped in `FakeStoreTheme` for each of the three `selectedDestination` values

## 5. ProductListViewModel — product_list_viewed Analytics (BDD)

- [ ] 5.1 Update the `createViewModel(...)` factory in `ProductListViewModelTest` to add an `analyticsClient: AnalyticsClient = mockk(relaxed = true)` parameter, threaded through to `ProductListViewModel(getProductsUseCase, analyticsClient)` (prerequisite for the tests below)
- [ ] 5.2 Write test: GIVEN a mocked `GetProductsUseCase` returning `Result.success` with a non-empty product list and a mocked `AnalyticsClient` WHEN `ProductListUiIntent.LoadProducts` is dispatched via `onIntent()` THEN the mocked `AnalyticsClient.logEvent()` is invoked exactly once with `name = "product_list_viewed"` and empty `params`, in `ProductListViewModelTest`
- [ ] 5.3 Write test: GIVEN a mocked `GetProductsUseCase` returning `Result.success` with an empty product list and a mocked `AnalyticsClient` WHEN `ProductListUiIntent.LoadProducts` is dispatched via `onIntent()` THEN the mocked `AnalyticsClient.logEvent()` is invoked exactly once with `name = "product_list_viewed"`, in `ProductListViewModelTest`
- [ ] 5.4 Write test: GIVEN a mocked `GetProductsUseCase` returning `Result.failure` and a mocked `AnalyticsClient` WHEN `ProductListUiIntent.LoadProducts` is dispatched via `onIntent()` THEN the mocked `AnalyticsClient.logEvent()` is never invoked with `name = "product_list_viewed"`, in `ProductListViewModelTest`
- [ ] 5.5 Write test: GIVEN a mocked `GetProductsUseCase` returning a fixed successful product list on every invocation and a mocked `AnalyticsClient` WHEN `ProductListUiIntent.LoadProducts` is dispatched twice via `onIntent()` THEN the mocked `AnalyticsClient.logEvent()` is invoked exactly twice with `name = "product_list_viewed"`, in `ProductListViewModelTest`
- [ ] 5.6 Write test: GIVEN a mocked `GetProductsUseCase` that returns `Result.failure` on its first invocation and `Result.success` with a product list on its second invocation, and a mocked `AnalyticsClient` WHEN `ProductListUiIntent.LoadProducts` is dispatched followed by `ProductListUiIntent.RetryClicked` THEN the mocked `AnalyticsClient.logEvent()` is invoked exactly once with `name = "product_list_viewed"`, in `ProductListViewModelTest`
- [ ] 5.7 Implement: add `private val analyticsClient: AnalyticsClient` to `ProductListViewModel`'s `@Inject constructor`; add a private companion `EVENT_PRODUCT_LIST_VIEWED = "product_list_viewed"` constant; call `analyticsClient.logEvent(EVENT_PRODUCT_LIST_VIEWED)` inside `loadProducts()`'s `result.onSuccess { ... }` branch, after setting `uiState` to `Content`

## 6. FavouritesScreen and ProfileScreen (BDD)

- [ ] 6.1 Write test: GIVEN `FavouritesScreen()` is composed THEN a text node showing the localised `favourites_placeholder` string is displayed, in `FavouritesScreenTest`
- [ ] 6.2 Implement: create `app/src/main/java/.../ui/screens/FavouritesScreen.kt` — a stateless `FavouritesScreen(modifier: Modifier = Modifier)` composable rendering a single centred `Text(stringResource(R.string.favourites_placeholder))`; add a `@Preview` `PreviewFavouritesScreen` wrapped in `FakeStoreTheme`
- [ ] 6.3 Write test: GIVEN `ProfileScreen()` is composed THEN a text node showing the localised `profile_placeholder` string is displayed, in `ProfileScreenTest`
- [ ] 6.4 Implement: create `app/src/main/java/.../ui/screens/ProfileScreen.kt` — a stateless `ProfileScreen(modifier: Modifier = Modifier)` composable rendering a single centred `Text(stringResource(R.string.profile_placeholder))`; add a `@Preview` `PreviewProfileScreen` wrapped in `FakeStoreTheme`

## 7. MainScreen — Navigation Host and Effect Handling (BDD)

- [ ] 7.1 Write test: GIVEN the stateless `MainScreen(uiState = MainUiState(), onIntent = {}, uiEffect = emptyFlow())` is composed for the first time THEN the bottom navigation bar is displayed with the Products tab selected, and the Products screen's content (its top app bar title) is visible, in `MainScreenTest`
- [ ] 7.2 Implement: build the stateless `MainScreen(uiState: MainUiState, onIntent: (MainUiIntent) -> Unit, uiEffect: Flow<MainUiEffect>, modifier: Modifier = Modifier)` composable — `rememberNavController()`, a `Scaffold` with `bottomBar = { BottomNavigationBar(selectedDestination = uiState.selectedDestination, onTabTapped = { onIntent(MainUiIntent.TabTapped(it)) }) }`, and a `NavHost(startDestination = AppDestination.Products)` with three `composable<AppDestination.*>` destinations hosting `ProductListScreen()`, `FavouritesScreen()`, `ProfileScreen()` respectively
- [ ] 7.3 Write test: GIVEN `MainScreen` is composed WHEN the user taps the Favourites tab in the bottom navigation bar THEN the `FavouritesScreen` placeholder text is displayed and the Favourites tab node reports a selected semantics state, in `MainScreenTest`
- [ ] 7.4 Write test: GIVEN `MainScreen` is composed and the Favourites tab is currently active WHEN the user taps the already-selected Favourites tab THEN the `FavouritesScreen` placeholder text remains displayed with no crash, in `MainScreenTest`
- [ ] 7.5 Implement: add a `LaunchedEffect(navController) { uiEffect.collect { effect -> when (effect) { is MainUiEffect.NavigateToTab -> navController.navigate(effect.destination) { popUpTo(navController.graph.findStartDestination().id) { saveState = true }; launchSingleTop = true; restoreState = true } } } }` inside `MainScreen`
- [ ] 7.6 Implement: add the stateful `MainScreen(modifier: Modifier = Modifier, viewModel: MainViewModel = hiltViewModel())` overload, collecting `viewModel.uiState` via `collectAsStateWithLifecycle()` and delegating to the stateless overload with `onIntent = viewModel::onIntent` and `uiEffect = viewModel.uiEffect`

## 8. Integration — MainActivity and Localisation

- [ ] 8.1 Update `MainActivity.kt` to call `MainScreen(modifier = Modifier.fillMaxSize())` (the stateful overload) instead of `ProductListScreen(modifier = Modifier.fillMaxSize())`, removing the now-unused `ProductListScreen` import
- [ ] 8.2 Add English string resources to `app/src/main/res/values/strings.xml`: `global_tab_products` ("Products"), `global_tab_favourites` ("Favourites"), `global_tab_profile` ("Profile"), `favourites_placeholder` ("Favourites coming soon…"), `profile_placeholder` ("Profile coming soon…")
- [ ] 8.3 Add the corresponding Spanish translations to `app/src/main/res/values-es/strings.xml`: `global_tab_products` ("Productos"), `global_tab_favourites` ("Favoritos"), `global_tab_profile` ("Perfil"), `favourites_placeholder` ("Favoritos disponibles próximamente…"), `profile_placeholder` ("Perfil disponible próximamente…")
- [ ] 8.4 Confirm the Hilt dependency graph resolves with no new DI modules required: `AnalyticsClient` is already provided as `@Singleton` by the existing `AnalyticsModule`, and both `MainViewModel` and `ProductListViewModel` are `@HiltViewModel`s with `@Inject` constructors, so `hiltViewModel()` in `MainScreen()` and `ProductListScreen()` resolves both without additional wiring

## 9. Final Verification

- [ ] 9.1 Run `./gradlew detektDebug` and resolve any violations
- [ ] 9.2 Run `./gradlew test` and confirm every `MainViewModelTest` and `ProductListViewModelTest` case passes
- [ ] 9.3 Run `./gradlew connectedDebugAndroidTest` and confirm every `BottomNavigationBarTest`, `FavouritesScreenTest`, `ProfileScreenTest`, and `MainScreenTest` case passes on a connected device or emulator
- [ ] 9.4 Run `./gradlew koverHtmlReportDebug` (or `koverVerify`) and confirm ≥95% coverage on `MainViewModel` and `ProductListViewModel`
- [ ] 9.5 Install and launch the app on a connected device/emulator; using `uiautomator dump` and screenshots, verify: the bottom navigation bar is visible on all three tabs; tapping Favourites/Profile switches content and visually highlights the tapped tab; tapping the already-selected tab produces no visible change; Products is selected by default at launch; scrolling the product list, switching to Favourites, and switching back preserves the prior scroll position
- [ ] 9.6 With the device/emulator still running, verify back-button behaviour: from Favourites or Profile, pressing back returns to Products; from Products, pressing back exits the app
- [ ] 9.7 Filter `adb logcat` for the `DebugAnalyticsProvider` tag and confirm: `product_list_viewed` logs once on the initial Products load and again after switching away to another tab and back; `tab_products_tapped`/`tab_favourites_tapped`/`tab_profile_tapped` each log on every tap of their respective tab, including repeated taps on an already-selected tab
- [ ] 9.8 Cross-check the implementation against every Acceptance Criterion in the `1.2.3 Navigation & Product List Analytics` user story, explicitly confirming the back-button convention (resolved via user clarification, not a standalone AC) behaves as described in 9.6
